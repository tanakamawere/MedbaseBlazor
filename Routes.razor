@using MedbaseComponents
@using MedbaseComponents.Pages
@using MedbaseComponents.Controls
<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(Routes).Assembly"
        AdditionalAssemblies="new[]{typeof(MedbaseComponents.Pages.Questions).Assembly, 
    typeof(MedbaseComponents.Pages.Topics).Assembly}">
        <Found Context="routeData">
            <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(WebLayout)" />
            <FocusOnNavigate RouteData="@routeData" Selector="h1" />
        </Found>
    </Router>
</CascadingAuthenticationState>
